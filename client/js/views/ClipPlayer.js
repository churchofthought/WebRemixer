WebRemixer.Views.ClipPlayer = Backbone.View.extend({
  className: 'clip-player',
    
  events: {
 
  },
  
  initialize: function(){
  
    _.bindAll(this);
    
    this.render();
  },

  render: function(){
    
  }
});